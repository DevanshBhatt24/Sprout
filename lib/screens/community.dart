import 'package:flutter/material.dart';

class ComunnityTab extends StatelessWidget {
  const ComunnityTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
